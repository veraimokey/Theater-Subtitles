'use stick'

class VerseContent {
    text;
    timeStart;
    timeFinish;

    constructor(text, timeStart, timeFinish) {
        this.text = text;
        this.timeStart = timeStart;
        this.timeFinish = timeFinish;


        // alert("" + timeStart + "!\n" + timeFinish + "!\n" + text);
    }

}
